cpsc 2720 - Assignment 3 
Test List
------------------------

A) FOR Tokenizer:
- XCheck that Union assigns and holds proper values
- XEnsure that crtTokenType returns accurate type dependant on received type
- XEnsure that crtTokenValue returns accurate value dependant on received value
- XEnsure that parse determines what the first token is in an expression
- XEnsure that nextToken is able to follow parse and determine exactly what the next token is
- XOverhaul Tokenizer Class more suited towards development
- Update all tests to ensure functionality
- Update tests to be more diverse / work with a greater range of items

B) FOR PostfixExpr:
- XCheck to ensure that a syntax error is thrown on a malformed equation
- XCheck to ensure that a lexical error is thrown on an equation
  with illegal characters
